import { Box } from '@/components/Box'
import { styled } from '@/styles'

export const Container = styled(Box, {
  margin: '$6 auto 0',
  padding: 0,
  display: 'grid',
  maxWidth: 480,
  position: 'relative',
})
